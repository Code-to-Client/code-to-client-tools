# Path to the shared SQLite database (used by both contact-form autofiller and this tracker).
# Default: ../contact-form/contacts.db (relative to workspace)
# DB_PATH=/path/to/contacts.db

# Comma-separated CORS origins for the frontend dev server.
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
